
service: lambda-auth # NOTE: update this with your service name

provider:
  name: aws
  runtime: nodejs6.10

functions:
  hello:
    handler: handler.test
    events:
      - http:
          path: /
          method: get
          cors: true


