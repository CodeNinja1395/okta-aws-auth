
service: lambda-auth # NOTE: update this with your service name

provider:
  name: aws
  runtime: nodejs6.10

functions:
  hello:
    handler: handler.test
    integration: lambda
    authorizer: auth
    events:
      - http:
          path: /
          method: get
          cors: true
  auth:
    handler: handler.auth

